part of 'imports_widgets_employee_details.dart';

class ActivitiesRecordBuilder extends StatelessWidget {
  const ActivitiesRecordBuilder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const UnAvailableBuilder(
      child: SizedBox(),
    );
  }
}
